#include <stdio.h>
 
int main() {
    
    int x = 10;
    int y = 3;

    int Soma = x + y;
    int Diferenca = x - y;
    int Produto = x * y;
    int Quociente = x / y;

 
// Exibição dos resultados
    printf("Soma: %d\n", soma);
    printf("Subtração: %d\n", diferenca);
    printf("Multiplicação: %d\n", produto);
    printf("Divisão: %d\n", quociente);

    return 0;

}